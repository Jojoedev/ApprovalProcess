@page
@model ApprovalProcess.Pages.Logics.DeptLogic.IndexModel
@{
}
<h3> </h3>
<a href="/Logics/DeptLogic/Create" class="btn btn-primary">Create</a>


@if(Model.departments.Count > 0)
{

             
<div>
    <table class="table table-bordered">

        <thead>
            <tr>
                <th>  List of Departments</th>
            </tr>
        </thead>
            <tbody class="col-3">
            @foreach (var item in Model.departments)
            {  <tr>
                <td> @item.Name </td>
                <td> <a href="/Edit?@item.Id" class="btn btn-outline-primary">Edit</a> </td>
                <td> <a href="/Logics/DeptLogic/Delete?id=@item.Id" onclick="return confirm('Do you want to delete this?')" class="btn btn-outline-primary">Delete</a> </td>
</tr>
            }
        </tbody>
    
        </table>
</div>  }
            else
            {
            <h3>No data exists! </h3>
            }

